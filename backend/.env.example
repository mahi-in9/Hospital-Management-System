NODE_ENV=development
PORT=5000

# MongoDB
MONGODB_URI=mongodb://localhost:27017/hospital-db

# JWT
JWT_SECRET=your-super-secret-key-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key
JWT_EXPIRY=1h
JWT_REFRESH_EXPIRY=7d

# Email Configuration (optional)
# If you provide SMTP credentials below, the app will use them to send real emails.
# If you leave them empty, the app will automatically create an Ethereal test account for development
# and log a preview URL where you can view the email (dev-only convenience).

# Example: Mailtrap (recommended for dev)
# EMAIL_HOST=smtp.mailtrap.io
# EMAIL_PORT=2525
# EMAIL_USER=your_mailtrap_user
# EMAIL_PASS=your_mailtrap_pass
# EMAIL_FROM="no-reply@yourdomain.com"

# Or real SMTP (production)
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM="no-reply@yourdomain.com"

# Frontend URL
FRONTEND_URL=http://localhost:5173
# If true, newly registered users will be ACTIVE immediately and receive tokens in the response.
# Useful for local development. Set to 'false' in production.
AUTO_ACTIVATE_USERS=false
